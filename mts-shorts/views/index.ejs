<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Site</title>
    <link rel="stylesheet" href="/styles.css">
</head>
    <body>
        <div style="text-align: center;">
            <p style="color: rgb(255, 68, 68)">Disclamer! Editing drops can give odd results. Try and get it correct so this doesn't occur.</p>
            <h1>MTS Shorts</h1>
            <a href="/shorts/new">Create a new short!</a>
            <%if (numOfShorts != 0){ %>
                <p>Total Shorts Reported - <%=numOfShorts%></p>
                <p>Total Shorts Resolved - <%=shortsResolved%></p>
                <p>Current Shorts - <%=numOfShorts-shortsResolved%></p>
            <%} %>
        </div>
        <hr>
        <ul>
            <% for (let drop in shorts[0]) { %>
                <li>
                    <div class="post-container">
                    <h2><%= drop%></h2>
                    <% shorts[0][drop].forEach(short => { %>
                        <div class="tyre-details">
                            <p> <%= short.size %></p>
                            <p> <%= short.rating %></p>
                            <p> <%= short.brand %></p>
                            <p> <%= short.type %></p>
                            <p> - Quantity - <%= short.quantity %></p>
                        </div>
                        <div class="button-container">
                            <form action="/shorts/<%=short.id%>/edit">
                                <input type="submit" value="Edit">
                            </form>
                            <form method="POST" action="/shorts/<%=short.id %>/delete">
                                <input type="submit" value="Delete">
                            </form>
                        </div>
                    </div>
                    <%})%>
                </li>   
            <% } %>
        </ul>
    </body>
</html>